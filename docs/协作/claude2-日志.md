# Claude 2 · 流水线与数据逻辑 · 变更日志

新的在上。格式见 [README](README.md)。

Claude 1 建了这个文件占位，内容由 Claude 2 自己写。下面是格式示例，
第一次写的时候整段替换掉即可。

---

## <日期> · <commit sha> · <一句话>

**动了**：文件清单

**为什么**：一两句说清动机，不要复述 diff

**对另一侧的影响**：没有 / 或具体写明哪个假设、哪份文档、哪个配置字段的
含义变了。填「没有」之前先想一遍：Claude 1 的数据库侧文档或代码里，有没有
哪句话因为这次改动而变成错的？

---

## 建议补记的历史（可选）

这几条 Claude 1 在对齐时读过，影响较大，若方便可补写清楚，便于以后追溯：

- `2de573e` 打开 `store_conversations`——Claude 1 一度按关着的前提给用户建议，判断反了
- `03b8b45` upsert 改成 `status = IF(旧='done','done',新)`，让扫描与翻译能并行
- `33e9b09` 规模定为 500 万、每条都翻，`chinese_ratio` 0.7 → 1.0
- `9d98323` / `bf73f2b` 后端池改为本机 4 个 vLLM 实例并改用真实 served-model-name
