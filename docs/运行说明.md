# finevision-to-sharegpt 运行说明

本文档说明三条配置化流水线和 `merge` 合并命令怎么运行、需要改哪些配置文件、输入输出分别是什么。

## 总览

三条流水线推荐直接走脚本：

```text
scripts/in_translate_json.sh
scripts/in_translate_zips.sh
scripts/in_export_zips.sh
```

脚本默认读取 `configs/` 下对应的配置文件，**直接编辑配置后无参运行即可**：

```bash
bash scripts/in_translate_json.sh   # 读 configs/translate_json.json
bash scripts/in_translate_zips.sh   # 读 configs/translate_zips.json
bash scripts/in_export_zips.sh      # 读 configs/export_zips.json
```

`merge` 不需要配置文件，直接用 CLI 指定输入和输出：

```bash
python -m finevision_to_sharegpt merge --inputs output/a.jsonl output/b.jsonl --output output/merged.jsonl
```

需要用别的配置时，把路径作为第一个参数传进去即可：

```bash
bash scripts/in_translate_zips.sh /path/to/my_translate_zips.json
```

脚本会自动：

- 切换到项目根目录（所以配置里的相对路径都相对项目根解析）；
- 设置 `PYTHONPATH=src`，**不需要安装包**就能运行；
- 用 `python -m finevision_to_sharegpt <command>` 调起对应功能。

可用环境变量 `PYTHON` 指定解释器，例如 `PYTHON=python3 bash scripts/in_translate_zips.sh`。

如果需要跨任务增量抽取（用多少抽多少），另有一组可选的 MySQL 脚本，见[功能四](#功能四可选接入-mysql-做消费账本)：

```bash
bash scripts/in_db_init.sh      # 建表，幂等
bash scripts/in_db_scan.sh      # 只灌源数据进库，不调模型
bash scripts/in_db_status.sh    # 看各状态计数
bash scripts/in_db_export.sh    # 从库里导出 ShareGPT
```

不配置 `mysql` 段时这组脚本用不到，三条主流水线的行为完全不变。

## 环境准备（迁移到服务器）

报错 `No module named finevision_to_sharegpt` 是因为包没被 Python 找到。两种解决方式，任选其一：

1. **用上面的脚本运行**（推荐，零安装）：脚本已设置 `PYTHONPATH=src`，直接能跑。
2. **正式安装**：在项目根目录执行一次

   ```bash
   pip install -e .
   ```

   之后 `finevision-to-sharegpt` 命令和 `python -m finevision_to_sharegpt` 都可用。

依赖安装：

```bash
pip install -r requirements.txt
```

> 注意：配置文件里的相对路径都相对**项目根目录**解析。脚本会自动 `cd` 到项目根；如果你手动跑 CLI，请在项目根目录下执行，或在配置里写绝对路径。

## 公共配置

### 多模型/多卡配置

涉及翻译的功能都需要 backend 配置，直接编辑：

```text
configs/backend_config.json
```

填入你的接口地址和 key。示例：

```json
{
  "request_timeout": 120,
  "max_retries": 2,
  "disable_backend_after_failures": 20,
  "backends": [
    {
      "name": "gpu0",
      "api_base": "http://10.0.0.1:18180/v1/chat/completions",
      "model": "Qwen3-VL",
      "api_key": "sk-local",
      "concurrency": 16,
      "weight": 1
    },
    {
      "name": "gpu1",
      "api_base": "http://10.0.0.2:18180/v1/chat/completions",
      "model": "Qwen3-VL",
      "api_key": "sk-local",
      "concurrency": 24,
      "weight": 1
    }
  ]
}
```

字段说明：

- `api_base`：大模型接口地址。
- `model`：模型名。
- `api_key`：接口 key。
- `concurrency`：该 backend 固定并发数。
- `request_timeout`：单次请求超时时间，单位秒。
- `max_retries`：失败后重试次数，`2` 表示最多尝试 3 次。
- `disable_backend_after_failures`：某个 backend 连续失败多少次后临时停用。

当前新配置路径使用固定并发，不再失败自动降并发。

### 提示词配置

默认提示词文件：

```text
prompts/translate_sample_zh.txt
prompts/translate_utterance_zh.txt
```

你可以直接改这两个文件，或在任务配置里指定别的：

```json
{
  "sample_prompt_file": "prompts/translate_sample_zh.txt",
  "utterance_prompt_file": "prompts/translate_utterance_zh.txt"
}
```

## 功能一：翻译已有 JSON

用途：你已经有英文 ShareGPT JSON/JSONL，只需要翻译文本。

这个功能不复制图片、不改图片路径，只读取原 JSON 里的 `images` 路径，把图片发给模型。

### 需要改的文件

直接编辑：

```text
configs/translate_json.json
```

默认内容（相对路径都相对项目根）：

```json
{
  "input": "data/input/english.jsonl",
  "output_jsonl": "output/chinese.jsonl",
  "output_json": "output/chinese.json",
  "done_path": "output/chinese.done.jsonl",
  "failed_path": "output/chinese.failed.jsonl",
  "images_root": "data/input",
  "backend_config": "configs/backend_config.json",
  "sample_prompt_file": "prompts/translate_sample_zh.txt",
  "utterance_prompt_file": "prompts/translate_utterance_zh.txt",
  "resume": true
}
```

### 输入

`input` 支持 JSON 数组或 JSONL。

每条数据格式：

```json
{
  "id": "sample-1",
  "images": ["images/a.jpg"],
  "conversations": [
    {"from": "human", "value": "<image>\nWhat is shown?"},
    {"from": "gpt", "value": "A chart."}
  ]
}
```

多图也支持：

```json
{
  "id": "sample-2",
  "images": ["images/a.jpg", "images/b.jpg"],
  "conversations": [
    {"from": "human", "value": "<image>\n<image>\nCompare these images."},
    {"from": "gpt", "value": "They show two charts."}
  ]
}
```

`images_root` 是图片相对路径的根目录。比如 JSON 里是 `images/a.jpg`，真实图片是 `data/input/images/a.jpg`，则：

```json
"images_root": "data/input"
```

### 运行

```bash
bash scripts/in_translate_json.sh
```

等价 CLI（需在项目根目录、且已设置 PYTHONPATH 或安装包）：

```bash
python -m finevision_to_sharegpt translate-json --config configs/translate_json.json
```

### 输出

```text
chinese.jsonl
chinese.json
chinese.done.jsonl
chinese.failed.jsonl
```

说明：

- `output_jsonl`：训练用 JSONL，流式写入。
- `output_json`：最终 JSON 数组，由 JSONL 汇总生成。
- `done_path`：断点续跑记录，已完成样本会跳过。
- `failed_path`：失败样本，不算完成，重跑时默认会再尝试。

### 断点续跑

`resume: true` 时会读取：

```text
done_path
output_jsonl
output_json
```

只要已有记录的 `id` 匹配，就会跳过，不会重新翻译你已经跑过的样本。

## 功能二：解压 zip 并按比例翻译

用途：直接从 zip/parquet 抽取图片和文本，按比例生成中文翻译样本和英文原样样本。

例如你要每个数据集 `70% 中文 + 30% 英文`，设置：

```json
"chinese_ratio": 0.7
```

比例按每个数据集单独执行，避免某些数据集全中文、某些数据集全英文。

### 需要改的文件

这个功能需要三个配置，直接编辑：

```text
configs/datasets.json
configs/backend_config.json
configs/translate_zips.json
```

### 数据集注册表

数据集有两种形式，可以混用。

**zip 形式**（压缩包，运行时解压到 `TMPDIR`）：

```json
{
  "data_root": "data/zips",
  "datasets": {
    "okvqa": { "zip": "okvqa.zip" },
    "captcha": { "zip": "captcha/captcha.zip" }
  }
}
```

**目录形式**（FineVision 风格，目录里是裸 parquet，**就地读取不解压、不占临时空间**）：

```json
{
  "data_root": "/mnt/data/FineVision",
  "datasets": {
    "CoSyn_400k_chart": { "dir": "CoSyn_400k_chart" },
    "arxivqa": { "dir": "arxivqa" }
  }
}
```

**多个数据根目录**：各个数据集合往往不在同一个父目录下，`data_root` 可以写成
数组，自动发现会逐个扫描：

```json
{
  "data_root": [
    "/mnt/data/mm_general/FineVision",
    "/mnt/data/mm_general/OmniScience",
    "/mnt/data/mm_general/SA1B-Dense-Caption"
  ],
  "auto_discover": true
}
```

同名数据集出现在两个根下会直接报错而不是静默取其一 —— 否则一半数据会无声消失。

注册之前先看看哪些集合本工具读得了：

```bash
python scripts/survey_roots.py /mnt/data/mm_general
```

它按目录统计文件类型和体积，分成「可直接注册（含 parquet 或 zip）」和
「需要先转换格式」两组。json、arrow、散图这类要先转成 parquet 才能进流水线。

**自动发现**：目录一多（FineVision 有几百个）就别手写了：

```json
{
  "data_root": "/mnt/data/FineVision",
  "auto_discover": true
}
```

`data_root` 下每个含有 parquet 的子目录注册成一个数据集，目录名即数据集名；根目录的 `README.md` 这类散文件、以及不含 parquet 的空目录都会被跳过。目录名里的括号等特殊字符不影响使用。

筛选用 `include` / `exclude`，按目录名精确匹配：

```json
{
  "data_root": "/mnt/data/FineVision",
  "auto_discover": { "exclude": ["docvqa", "unigeo(mathv360k)"] }
}
```

显式写在 `datasets` 里的条目会覆盖同名的自动发现结果，可以用来给某个数据集指定别处的路径。

确认发现了哪些：

```bash
PYTHONPATH=src python -m finevision_to_sharegpt list-datasets \
  --config configs/translate_zips.json --limit 10
```

输出每个数据集的名字、形式（dir/zip）、parquet 分片数和体积，末尾有 `total`。

**务必核对 `total`**：如果预期几百个数据集却只显示 1 个，说明 `data_root` 指到了上一层 —— 父目录里递归能搜到 parquet，于是它整个被当成了一个数据集。往下再指一级即可。

`sample_id` 的构成随形式而变：zip 是 `<zip名去掉后缀>:<成员路径>:<行号>`，目录是 `<目录名>:<相对路径>:<行号>`。已经跑过的 zip 任务 id 不变，可以照常续跑。

### 任务配置

`configs/translate_zips.json` 示例：

```json
{
  "dataset_registry": "configs/datasets.json",
  "datasets": [
    "okvqa",
    {
      "name": "captcha",
      "chinese_ratio": 1.0,
      "limit": 5000
    }
  ],
  "output_jsonl": "output/train.jsonl",
  "chinese_ratio": 1.0,
  "seed": 42,
  "limit_per_dataset": null,
  "backend_config": "configs/backend_config.json",
  "sample_prompt_file": "prompts/translate_sample_zh.txt",
  "utterance_prompt_file": "prompts/translate_utterance_zh.txt",
  "emit_raw": true,
  "resume": true
}
```

> 想全部翻成中文就把 `chinese_ratio` 设为 `1.0`（默认配置即如此）；想保留一部分英文再调小，例如 `0.7` 表示每个数据集约 70% 中文、30% 英文。

`output_json`、`images_root`、`failed_path`、`rejected_path`、`report_path` 可省略；默认会按 `output_jsonl` 所在目录推导为 `train.json`、`images/`、`failed.jsonl`、`rejected.jsonl`、`report.json`。

字段说明：

- `dataset_registry`：数据集注册表路径。
- `datasets`：要处理的数据集名字。
- `chinese_ratio`：默认中文比例。
- `seed`：稳定分流 seed，保证断点重跑同一样本仍落到同一种语言。
- `limit_per_dataset`：每个数据集最多输出多少条，`null` 表示不限制。
- `emit_raw`：是否逐数据集写出翻译前英文 `raw.jsonl`，默认 `true`。
- 单个数据集可覆盖比例和数量：

```json
{
  "name": "captcha",
  "chinese_ratio": 1.0,
  "limit": 5000
}
```

### 输入

输入是注册表里的 zip 文件：

```text
data/zips/okvqa.zip
data/zips/chartqa.zip
data/zips/captcha/captcha.zip
```

zip 内部应包含 parquet 文件。程序会从 parquet row 里读取：

- `images` 或 `image`
- `texts`、`messages`、`conversations`
- 如果没有对话，会尝试 `caption`、`description`、`text`

### 运行

```bash
bash scripts/in_translate_zips.sh
```

等价 CLI：

```bash
python -m finevision_to_sharegpt translate-zips --config configs/translate_zips.json
```

### 输出

```text
output/train.jsonl                          # 汇总层：所有数据集的翻译层
output/train.json
output/<dataset_name>/raw.jsonl             # 原始层：翻译前英文记录
output/<dataset_name>/train.jsonl           # 翻译层：中文与保留英文的混合
output/<dataset_name>/train.json
output/images/<dataset_name>/<hash>.<ext>
output/failed.jsonl
output/rejected.jsonl
output/report.json
```

> 每个数据集默认会产出翻译前的 `raw.jsonl`，以及翻译后的 `train.json` / `train.jsonl`；根目录的 `train.json` / `train.jsonl` 是所有数据集翻译层的汇总。`failed.jsonl` / `rejected.jsonl` / `report.json` 仍是单一汇总文件，不生成跨数据集 raw 汇总。

示例记录：

```json
{
  "id": "okvqa:nested/part.parquet:0",
  "images": [
    "images/okvqa/abc123.jpg",
    "images/okvqa/def456.png"
  ],
  "conversations": [
    {
      "from": "human",
      "value": "<image>\n<image>\n问题"
    },
    {
      "from": "gpt",
      "value": "答案"
    }
  ]
}
```

说明：

- 中文样本会调用模型翻译。
- 英文样本不调用模型，直接输出原始英文 ShareGPT。
- 多图会在第一条 human 前插入对应数量的 `<image>`。
- 图片只在 zip 抽取链路里写到 `images/<dataset_name>/`。
- raw 与 train 共用相同的 `images` 路径，图片只保存一次；raw 只生成 JSONL，不生成 `raw.json`。

### 断点续跑

`resume: true` 时会扫描 `output_jsonl` 中已有 `id`，已有样本跳过。

zip 样本 id 规则：

```text
{zip_stem}:{parquet_name}:{row_index}
```

失败样本写入 `failed_path`，默认不算完成，重跑会再次尝试。

raw 使用独立的逐数据集 `id` 去重集合，因此翻译失败后 resume 重试不会重复写 raw。已经完成的样本会在解析前跳过；如果旧任务中途才开启 `emit_raw`，之前完成的英文原文不会自动回填。需要完整 raw 时请设置 `resume: false` 从头运行，或从任务开始就保持 `emit_raw: true`。

## 功能三：解压 zip 不翻译，直接英文导出

用途：从 zip/parquet 抽取图片和英文文本，生成 ShareGPT，不调用模型。

### 需要改的文件

这个功能需要两个配置，直接编辑：

```text
configs/datasets.json
configs/export_zips.json
```

不需要 `backend_config.json`。

### 任务配置

`configs/export_zips.json` 示例：

```json
{
  "dataset_registry": "configs/datasets.json",
  "datasets": ["*"],
  "output_jsonl": "output/train_en.jsonl",
  "limit_per_dataset": null,
  "resume": true
}
```

`output_json`、`images_root`、`rejected_path`、`report_path` 可省略；默认从 `output_jsonl` 推导。

### 输入

同功能二，输入是 `datasets.json` 注册的 zip/parquet 数据集。

### 运行

```bash
bash scripts/in_export_zips.sh
```

等价 CLI：

```bash
python -m finevision_to_sharegpt export-zips --config configs/export_zips.json
```

### 输出

```text
output/train_en.jsonl                       # 所有数据集汇总
output/train_en.json
output/<dataset_name>/train_en.jsonl        # 每个数据集单独一份
output/<dataset_name>/train_en.json
output/images/<dataset_name>/<hash>.<ext>
output/rejected.jsonl
output/report.json
```

和功能二一样，每个数据集单独出一份、外加一份汇总。这个功能不会生成 `raw.jsonl` 或 `failed.jsonl`：其 `train_en.jsonl` 本身就是抽取结果，而且不调用模型。解析不了的样本会进入 `rejected.jsonl`。

## 功能四（可选）：接入 MySQL 做消费账本

### 解决什么问题

文件模式下 `resume` 只认**本次** `output_jsonl` 里的 `id`。换一个输出目录跑第二批，会从第 0 行重新抽起，两批数据重叠。接上 MySQL 后：

- 换输出目录、换机器都不会重复抽到用过的样本；
- 记录每个 parquet 扫到第几行，重跑按 row group 整组跳过，不再空转前面几百万行；
- zip 换版（内容变了）自动算新版本，历史记录不会被误判；
- 源数据和译文分表存放，可按 `source_id` 关联查询。

### 需要改的文件

```text
configs/mysql.json
```

或者直接在 `configs/translate_zips.json` / `configs/export_zips.json` 里加一个 `mysql` 段。

### 任务配置

```json
{
  "dataset_registry": "configs/datasets.json",
  "datasets": ["*"],
  "output_jsonl": "output/train.jsonl",
  "limit_per_dataset": 5000,
  "backend_config": "configs/backend_config.json",
  "batch_id": null,
  "mysql": {
    "host": "10.0.0.5",
    "port": 3306,
    "user": "fv",
    "password": "${FV_MYSQL_PASSWORD}",
    "database": "finevision",
    "charset": "utf8mb4",
    "collation": "utf8mb4_0900_ai_ci",
    "batch_size": 200,
    "flush_interval_seconds": 5,
    "claim_ttl_seconds": 3600,
    "on_connect_error": "fallback"
  }
}
```

字段说明：

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `password` | 空 | 支持 `${环境变量}` 展开，别把密码写进 git |
| `collation` | `utf8mb4_0900_ai_ci` | 目标是 MySQL 8.0；服务端没这个排序规则时改掉它 |
| `batch_size` | 200 | 攒够多少条写一次库 |
| `flush_interval_seconds` | 5 | 攒批的最长等待时间，和 `batch_size` 先到先 flush |
| `claim_ttl_seconds` | 3600 | 认领有效期。进程崩掉后，超时的认领在下次运行时自动回收重抽 |
| `on_connect_error` | `fallback` | `fallback` 连不上就降级回文件模式继续跑；`fail` 直接报错退出 |
| `batch_id` | 按时间戳生成 | 标记这一批是哪次任务抽的，`db-export` 可按它筛选 |

### 建表

```bash
bash scripts/in_db_init.sh
```

幂等操作，重复跑没有副作用。建四张表加每个已注册数据集的视图。

### 用多少抽多少

`limit_per_dataset` 的语义是**本次新增 N 条**，不是累计上限 N 条。配好之后重复运行即可：

```bash
bash scripts/in_translate_zips.sh   # 抽 5000 条
bash scripts/in_translate_zips.sh   # 再抽 5000 条全新的
```

两次的 `output_jsonl` 可以指向不同目录，样本也不会重复。

### 只灌库不翻译

想先把源数据沉淀成资产，之后再慢慢消费：

```bash
PYTHONPATH=src python -m finevision_to_sharegpt db-scan --config configs/db_scan.json --no-images
```

**注意 `limit_per_dataset`**：`db-scan` 同样受它限制。翻译配置里通常会设一个
较小的值（比如 100）用来控制每次抽多少，但灌库要的是全量，所以仓库里单独
放了 `configs/db_scan.json`，其中 `limit_per_dataset` 是 `null`。拿翻译配置
直接灌库，每个数据集只会进 100 条。

灌库是**可断点续传**的：中途挂掉后重跑，已经入库的部分一行都不会重读
（按 `dataset_cursor` 的水位线直接跳过）。这一点和消费路径的语义不同 ——
消费时 `pending` 行正是要找的目标，所以不能跳；灌库时水位线以下的行无论
什么状态都已在库里，可以整段跳过。

#### 大规模灌库前先试点

`db-scan` 要把 parquet **全读一遍**才能抽出文本。一棵几 TB 的树意味着数小时
的顺序读，以及一个不小的 MySQL 库，值得先用一两个数据集量一下再决定全量。

```bash
# 1) 看清楚规模
PYTHONPATH=src python -m finevision_to_sharegpt list-datasets --config configs/db_scan.json --limit 5

# 2) 试点：configs/db_scan_pilot.json 里 datasets 只列了一个数据集
time PYTHONPATH=src python -m finevision_to_sharegpt db-scan \
     --config configs/db_scan_pilot.json --no-images

# 3) 量库占用，据此外推
PYTHONPATH=src python -m finevision_to_sharegpt db-storage --config configs/db_scan_pilot.json
```

`db-storage` 会给出每张表的大小、`sample_source` 的准确行数和 `bytes_per_row`。
样本太小时它会提示这个数字不可外推（InnoDB 页开销占主导），**至少扫到十万行
量级再外推**。

拿试点的「耗时 / GB」和「库占用 / 行」乘上 `list-datasets` 给出的总量，就知道
全量要多久、要多少库空间。别忘了 `df -h` 看一眼 MySQL datadir 所在卷。

如果只打算训练其中一部分数据集，就别全量灌 —— 在 `configs/datasets.json` 里
用 `auto_discover.include` 圈定范围，或直接在任务配置的 `datasets` 里列出来，
省下的是按 TB 计的 I/O。

#### 多进程并行灌库

灌库是按字节走的，且约有一半时间在等 I/O，所以拆成几个进程跑会快不少。
按数据集切分不需要任何协调：两个进程碰不到同一行，账本的认领机制本来就
容忍并发写入。

```bash
python scripts/split_scan_configs.py configs/db_scan.json 6 -o configs/shards
```

按体积做最长优先分配，让各分片的完成时间尽量接近，然后打印出可直接粘贴的
并发启动命令。每个分片写同一个库，最后 `db-status` 汇总即可。

注意 `nohup` 要用 `env` 把密码传进子进程：

```bash
nohup env FV_MYSQL_PASSWORD="$FV_MYSQL_PASSWORD" python -m finevision_to_sharegpt db-scan --config configs/shards/shard_00.json --no-images > configs/shards/shard_00.log 2>&1 &
```

并发数别超过存储的并行能力，超了只会互相抢 I/O。

灌进去的行状态是 `pending`，不生成任何 ShareGPT 输出文件。之后跑
`translate-zips` 或 `export-zips` 时会正常认领这些行。

#### 图片要不要一起抽出来

默认会抽。但如果你只打算用整棵树的一小部分，先抽全部图片是巨大的浪费 ——
加 `--no-images`：

```bash
PYTHONPATH=src python -m finevision_to_sharegpt db-scan --config configs/translate_zips.json --no-images
```

这样只把文本和图片路径写进库，**不落盘任何图片文件**。之所以可行，是因为
图片文件名就是内容的 sha256，而扫描时字节本来就在内存里 —— 路径不读盘也能
算准。之后哪次运行真正消费了某条样本，就在那时落盘它的图片。

两种方式的取舍：

| | 默认（抽图） | `--no-images` |
| --- | --- | --- |
| 扫描耗时 | 慢，受写盘限制 | 快，只算哈希 |
| 扫描占用磁盘 | 全量图片 | 0 |
| 后续消费 | 图片已就位 | 消费时才落盘 |
| 适合 | 确定要全量用 | 用多少抽多少 |

无论哪种，库里记的 `image_paths` 都是最终正确的路径，不会对不上。

**训练前图片必须存在**：LLaMA-Factory 按路径读图。用 `--no-images` 扫描后，
只有被 `translate-zips` / `export-zips` 消费过的样本才有图，这也正是你要拿去
训练的那部分。

### 导出与查看

```bash
bash scripts/in_db_status.sh                                  # 各数据集各状态计数
bash scripts/in_db_export.sh                                  # 全量导出
bash scripts/in_db_export.sh configs/mysql.json --dataset okvqa --lang zh
bash scripts/in_db_export.sh configs/mysql.json --batch-id 20260828-101500
```

导出路径默认是 `output/db_export.jsonl`，用环境变量 `FV_DB_EXPORT_OUTPUT` 改。同时会生成对应的 `.json` 数组。

同一条源数据允许存在多条译文（换模型或换提示词重翻各存一条），导出时取 `created_at` 最新的那条。

### 库会占多少磁盘，占在哪

`sample_source` 默认会存**英文正文**（`conversations` 列），这是磁盘占用的大头。
图片不进库，但每条样本的路径要存（一张图约 90 字节）。用 `db-storage` 看实测：

```bash
PYTHONPATH=src python -m finevision_to_sharegpt db-storage --config configs/db_scan.json
```

`avg_bytes_by_column` 会分别给出 `conversations` / `image_paths` / `identifiers`
三部分的每行平均字节，`text_share_of_payload` 是正文占载荷的比例。乘以总行数
就是全量的量级。

#### 只要映射，不要正文

如果你只把库当作「抽过哪些、抽到哪了」的账本，正文完全可以不存：

```json
{ "mysql": { "store_conversations": false } }
```

| | 默认（存正文） | `store_conversations: false` |
| --- | --- | --- |
| 库占用 | 正文通常是大头 | 只剩 id、图片路径、状态 |
| 增量抽取、去重、断点续传 | 正常 | **完全一样** |
| `translate-zips` / `export-zips` | 正常 | **完全一样**（正文从 parquet 读，不从库读） |
| `db-export` 导出**已翻译**样本 | 可以 | **可以**（译文在 `sample_translation`，不受影响） |
| `db-export` 导出**未翻译的英文**样本 | 可以 | 不可以，会被跳过并附说明 |

关键点是：**关掉它不影响任何一条流水线**。正文本来就是从 parquet 现读的，
库里那份只是给 `db-export` 用的英文副本。译文存在 `sample_translation` 里，
不受这个开关影响，所以翻译过的样本照样能用 `db-export` 导出。

真正失去的只有一件事：把**没翻译过的英文样本**从库里重建出来。而这些样本
本来也会写进 `output/*.jsonl`，需要时从那里拿即可。

改了这个开关不会自动重写已有的行；想换模式就清库重扫。

### 表结构

```text
dataset_version   ── zip 指纹，一个 zip 版本一行
      ├─< sample_source      ── 源数据大表：英文原文、图片路径、消费状态
      │         └─< sample_translation  ── 译文，靠 source_id 关联
      └─< dataset_cursor     ── 每个 parquet 扫到哪一行的水位线
```

每个数据集另有一个视图 `v_sample_source_<数据集名>`，查询体验和小表一致，但物理上只有 `sample_source` 一张表，加数据集不需要建表。

`sample_source.status` 的取值：

| 状态 | 含义 |
| --- | --- |
| `pending` | 已入库未消费（`db-scan` 灌进来的，或过期认领被回收的） |
| `claimed` | 已被抽走，正在处理中；超过 `claim_ttl_seconds` 会被回收 |
| `done` | 已完成，`lang_assigned` 记录是中文还是英文 |
| `failed` | 翻译失败，下次运行会重试 |
| `rejected` | 解析被拒（缺图或缺文本），不再重试 |

`sample_translation` 会记下 `backend_name`、`model_name`、`prompt_version` 和 `latency_ms`，所以可以查“哪个模型翻得慢/翻得差”再按模型定向重翻。

图片**不进库**，仍按 `images/<数据集名>/<hash>.<ext>` 落盘，表里只存相对路径。这样库不会被 BLOB 撑爆，训练时也照旧按路径读图。

### 崩溃恢复

翻译是异步的：样本被抽出后要等模型返回才落盘。如果进程中途被 kill：

- 这些样本在库里是 `claimed` 状态，带一个过期时间；
- 下次运行时，超过 `claim_ttl_seconds` 的认领会自动回收成 `pending`，重新参与抽取；
- 所以不会出现“抽出来了但没落盘、以后再也抽不到”的黑洞。

如果 `claim_ttl_seconds` 设得比单条样本的翻译耗时还短，会出现重复翻译。默认 3600 秒对单条样本足够宽松。

### 本地起一个测试库

```bash
docker compose up -d mysql
```

跑集成测试（不设这个环境变量就自动跳过）：

```bash
FV_TEST_MYSQL='{"host":"127.0.0.1","port":3306,"user":"fv","password":"fv","database":"finevision"}' \
  pytest tests/test_db_integration.py
```

## 按类别配额抽样

想要的往往不是「每个数据集各抽 N 条」，而是「caption 占 37.5%、通用 VQA 占
20%……」这样的类别配比。流水线本来就支持给每个数据集单独设 `limit`，所以这是
一个规划问题：先算出每个数据集该出多少条，再把数字写进配置。

### 一、先看清楚库里到底有哪些数据集

```bash
python scripts/plan_sampling.py --config configs/db_scan.json --dump
```

按可用行数倒序列出所有数据集。**这是唯一可靠的名字来源** —— 别照着别处抄。

### 二、写配额计划

`configs/sampling_plan.json`：

```json
{
  "total": 175000,
  "categories": {
    "caption_图文描述": { "share": 0.375, "patterns": ["caption", "^sharegpt4[ov]", "textcaps"] },
    "vqa_通用问答":     { "share": 0.20,  "patterns": ["^vqav2$", "^a_okvqa$"] }
  }
}
```

- `share` 是类别占 `total` 的比例。
- `patterns` 是对数据集名的正则（不区分大小写），`match` 是精确名，可混用。
  **优先用精确名** —— 名字来自 `--dump` 的真实输出，正则容易误伤。
- **一个数据集只会被第一个匹配到的类别认领**，不会重复计数。
- `max_share_per_dataset` 限制单个数据集在本类别中的占比上限。不设的话，
  百万行级的数据集会按比例独吞大半配额（`densefusion_1m` 一家就能供上
  caption 类的一半），混合度反而变差。
  注意上限太紧会填不满：N 个数据集各限 `1/N` 以下，总和就够不到目标，
  这时会标 `short by N`。

### 三、生成任务配置

```bash
python scripts/plan_sampling.py --config configs/db_scan.json --plan configs/sampling_plan.json -o configs/translate_pretrain.json
```

输出每个类别的目标数、实得数、涉及数据集数；某类别没匹配到任何数据集会标
`NO MATCH`，池子不够会标 `short by N`。**没被任何类别匹配的数据集会单独列出**，
不会悄悄漏掉。

配额在类别内按各数据集的可用行数**按比例分配**，且任何数据集都不会超抽；小池
被抽满后，缺口自动由还有余量的数据集补上。

生成的配置里 `limit_per_dataset` 会被移除（否则会盖掉每数据集的配额），
`chinese_ratio` 设为 1.0。

### 四、跑翻译

```bash
bash scripts/in_translate_zips.sh configs/translate_pretrain.json
```

### 抽样是「按顺序取前 N 条」，不是随机

需要注意：流水线从每个数据集的**起始位置顺序取满 N 条**就停。多数数据集的行
本来就是乱序的，这样取近似随机；但如果某个数据集按类别或难度排过序，取前 N
条就会有偏。

要真随机的话有两条路，都比顺序取贵：

- 全量扫一遍，用 `sample_id` 的稳定哈希做分流（和 `chinese_ratio` 同一套机制）。
  好处是一遍过、可复现；代价是即使只要 1% 也要读完整棵树。
- 用库里的 `sample_id` + `row_index` 随机选中目标行，再只读包含这些行的
  row group。抽取比例低时省得多，比例高时和全量扫差不多。

## 合并中间 JSONL

`merge` 直接读取一个或多个 JSONL，不使用配置文件：

```bash
python -m finevision_to_sharegpt merge \
  --inputs output/okvqa/train.jsonl output/chartqa/train.jsonl \
  --output output/merged.jsonl
```

行为规则：

- 严格保持 `--inputs` 顺序以及每个文件内部的记录顺序。
- 相同 `id` 只保留第一次出现的记录；没有 `id`（或 `id` 为 `null`）的记录全部保留。
- 不排序、不打乱、不抽样，不改变记录内容。
- 覆盖写出 `output/merged.jsonl`，并自动生成同名 `output/merged.json` 数组。

完成后会打印：

```text
read=<总读入> written=<去重后> duplicates=<去掉的重复数> output=<输出路径>
```

## 多图规则

所有输出统一：

- `images` 数组里有几张图，第一条 human 里就放几个 `<image>`。
- token 连续放在第一条 human 开头。

示例：

```text
<image>
<image>
问题
```

对应：

```json
"images": ["images/a.jpg", "images/b.jpg"]
```

## 低内存和速度设计

当前实现：

- parquet 流式读取。
- backend pool 使用固定并发和有界任务队列，不一次性吃完整数据集。
- 成功结果立即写 JSONL。
- 最终 JSON 从 JSONL 二次流式汇总生成。
- `translate-json` 不搬图片，只读原路径。
- `translate-zips` 和 `export-zips` 才会抽图到 `images/<dataset_name>/`。
- 三个主功能都有 tqdm 进度条；`translate-json` 显示记录总进度，`translate-zips` 和 `export-zips` 显示每个 parquet 的 row 进度，并在后缀展示 processed/written/failed/skipped 等统计。

速度来自：

- 多线程请求。
- 每个 backend 固定并发。
- 多卡注册为多个 backend。
- 多模型注册为多个 backend。
- backend 从共享任务流里动态取任务，快 backend 会自然处理更多样本。

## 常见运行顺序

### 已有 JSON 翻译

1. 编辑 `configs/backend_config.json`
2. 编辑 `configs/translate_json.json`
3. 确认 `images_root` 能找到 JSON 里的图片路径
4. 运行：

```bash
bash scripts/in_translate_json.sh
```

### zip 按比例翻译

1. 编辑 `configs/datasets.json`
2. 编辑 `configs/backend_config.json`
3. 编辑 `configs/translate_zips.json`
4. 运行：

```bash
bash scripts/in_translate_zips.sh
```

### zip 英文导出

1. 编辑 `configs/datasets.json`
2. 编辑 `configs/export_zips.json`
3. 运行：

```bash
bash scripts/in_export_zips.sh
```

### 合并中间文件

```bash
python -m finevision_to_sharegpt merge \
  --inputs output/okvqa/train.jsonl output/chartqa/train.jsonl \
  --output output/merged.jsonl
```

### 接了 MySQL 的增量抽取

1. 编辑 `configs/datasets.json`
2. 编辑 `configs/backend_config.json`
3. 在 `configs/translate_zips.json` 里加 `mysql` 段和 `limit_per_dataset`
4. 建表（只需一次）：

```bash
bash scripts/in_db_init.sh configs/translate_zips.json
```

5. 之后每次要新数据就重复运行，抽多少给多少：

```bash
bash scripts/in_translate_zips.sh
bash scripts/in_db_status.sh configs/translate_zips.json
```

## 快速检查输出

检查 JSONL 行数：

```bash
wc -l output/train.jsonl
```

检查 failed：

```bash
wc -l output/failed.jsonl
```

检查 report：

```bash
cat output/report.json
```

校验 ShareGPT 格式：

```bash
python -m finevision_to_sharegpt validate \
  --input output/train.jsonl \
  --output output/train.clean.jsonl \
  --rejects output/train.format_rejected.jsonl
```
