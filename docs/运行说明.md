# finevision-to-sharegpt 运行说明

本文档说明三条配置化流水线和 `merge` 合并命令怎么运行、需要改哪些配置文件、输入输出分别是什么。

## 总览

三条流水线推荐直接走脚本：

```text
scripts/in_translate_json.sh
scripts/in_translate_zips.sh
scripts/in_export_zips.sh
```

脚本默认读取 `configs/` 下对应的配置文件，**直接编辑配置后无参运行即可**：

```bash
bash scripts/in_translate_json.sh   # 读 configs/translate_json.json
bash scripts/in_translate_zips.sh   # 读 configs/translate_zips.json
bash scripts/in_export_zips.sh      # 读 configs/export_zips.json
```

`merge` 不需要配置文件，直接用 CLI 指定输入和输出：

```bash
python -m finevision_to_sharegpt merge --inputs output/a.jsonl output/b.jsonl --output output/merged.jsonl
```

需要用别的配置时，把路径作为第一个参数传进去即可：

```bash
bash scripts/in_translate_zips.sh /path/to/my_translate_zips.json
```

脚本会自动：

- 切换到项目根目录（所以配置里的相对路径都相对项目根解析）；
- 设置 `PYTHONPATH=src`，**不需要安装包**就能运行；
- 用 `python -m finevision_to_sharegpt <command>` 调起对应功能。

可用环境变量 `PYTHON` 指定解释器，例如 `PYTHON=python3 bash scripts/in_translate_zips.sh`。

## 环境准备（迁移到服务器）

报错 `No module named finevision_to_sharegpt` 是因为包没被 Python 找到。两种解决方式，任选其一：

1. **用上面的脚本运行**（推荐，零安装）：脚本已设置 `PYTHONPATH=src`，直接能跑。
2. **正式安装**：在项目根目录执行一次

   ```bash
   pip install -e .
   ```

   之后 `finevision-to-sharegpt` 命令和 `python -m finevision_to_sharegpt` 都可用。

依赖安装：

```bash
pip install -r requirements.txt
```

> 注意：配置文件里的相对路径都相对**项目根目录**解析。脚本会自动 `cd` 到项目根；如果你手动跑 CLI，请在项目根目录下执行，或在配置里写绝对路径。

## 公共配置

### 多模型/多卡配置

涉及翻译的功能都需要 backend 配置，直接编辑：

```text
configs/backend_config.json
```

填入你的接口地址和 key。示例：

```json
{
  "request_timeout": 120,
  "max_retries": 2,
  "disable_backend_after_failures": 20,
  "backends": [
    {
      "name": "gpu0",
      "api_base": "http://10.0.0.1:18180/v1/chat/completions",
      "model": "Qwen3-VL",
      "api_key": "sk-local",
      "concurrency": 16,
      "weight": 1
    },
    {
      "name": "gpu1",
      "api_base": "http://10.0.0.2:18180/v1/chat/completions",
      "model": "Qwen3-VL",
      "api_key": "sk-local",
      "concurrency": 24,
      "weight": 1
    }
  ]
}
```

字段说明：

- `api_base`：大模型接口地址。
- `model`：模型名。
- `api_key`：接口 key。
- `concurrency`：该 backend 固定并发数。
- `request_timeout`：单次请求超时时间，单位秒。
- `max_retries`：失败后重试次数，`2` 表示最多尝试 3 次。
- `disable_backend_after_failures`：某个 backend 连续失败多少次后临时停用。

当前新配置路径使用固定并发，不再失败自动降并发。

### 提示词配置

默认提示词文件：

```text
prompts/translate_sample_zh.txt
prompts/translate_utterance_zh.txt
```

你可以直接改这两个文件，或在任务配置里指定别的：

```json
{
  "sample_prompt_file": "prompts/translate_sample_zh.txt",
  "utterance_prompt_file": "prompts/translate_utterance_zh.txt"
}
```

## 功能一：翻译已有 JSON

用途：你已经有英文 ShareGPT JSON/JSONL，只需要翻译文本。

这个功能不复制图片、不改图片路径，只读取原 JSON 里的 `images` 路径，把图片发给模型。

### 需要改的文件

直接编辑：

```text
configs/translate_json.json
```

默认内容（相对路径都相对项目根）：

```json
{
  "input": "data/input/english.jsonl",
  "output_jsonl": "output/chinese.jsonl",
  "output_json": "output/chinese.json",
  "done_path": "output/chinese.done.jsonl",
  "failed_path": "output/chinese.failed.jsonl",
  "images_root": "data/input",
  "backend_config": "configs/backend_config.json",
  "sample_prompt_file": "prompts/translate_sample_zh.txt",
  "utterance_prompt_file": "prompts/translate_utterance_zh.txt",
  "resume": true
}
```

### 输入

`input` 支持 JSON 数组或 JSONL。

每条数据格式：

```json
{
  "id": "sample-1",
  "images": ["images/a.jpg"],
  "conversations": [
    {"from": "human", "value": "<image>\nWhat is shown?"},
    {"from": "gpt", "value": "A chart."}
  ]
}
```

多图也支持：

```json
{
  "id": "sample-2",
  "images": ["images/a.jpg", "images/b.jpg"],
  "conversations": [
    {"from": "human", "value": "<image>\n<image>\nCompare these images."},
    {"from": "gpt", "value": "They show two charts."}
  ]
}
```

`images_root` 是图片相对路径的根目录。比如 JSON 里是 `images/a.jpg`，真实图片是 `data/input/images/a.jpg`，则：

```json
"images_root": "data/input"
```

### 运行

```bash
bash scripts/in_translate_json.sh
```

等价 CLI（需在项目根目录、且已设置 PYTHONPATH 或安装包）：

```bash
python -m finevision_to_sharegpt translate-json --config configs/translate_json.json
```

### 输出

```text
chinese.jsonl
chinese.json
chinese.done.jsonl
chinese.failed.jsonl
```

说明：

- `output_jsonl`：训练用 JSONL，流式写入。
- `output_json`：最终 JSON 数组，由 JSONL 汇总生成。
- `done_path`：断点续跑记录，已完成样本会跳过。
- `failed_path`：失败样本，不算完成，重跑时默认会再尝试。

### 断点续跑

`resume: true` 时会读取：

```text
done_path
output_jsonl
output_json
```

只要已有记录的 `id` 匹配，就会跳过，不会重新翻译你已经跑过的样本。

## 功能二：解压 zip 并按比例翻译

用途：直接从 zip/parquet 抽取图片和文本，按比例生成中文翻译样本和英文原样样本。

例如你要每个数据集 `70% 中文 + 30% 英文`，设置：

```json
"chinese_ratio": 0.7
```

比例按每个数据集单独执行，避免某些数据集全中文、某些数据集全英文。

### 需要改的文件

这个功能需要三个配置，直接编辑：

```text
configs/datasets.json
configs/backend_config.json
configs/translate_zips.json
```

### 数据集注册表

`configs/datasets.json` 示例（`data_root` 相对项目根）：

```json
{
  "data_root": "data/zips",
  "datasets": {
    "okvqa": {
      "zip": "okvqa.zip"
    },
    "chartqa": {
      "zip": "chartqa.zip"
    },
    "captcha": {
      "zip": "captcha/captcha.zip"
    }
  }
}
```

之后任务配置里只写数据集名字：

```json
"datasets": ["okvqa", "chartqa"]
```

跑全部注册数据集：

```json
"datasets": ["*"]
```

### 任务配置

`configs/translate_zips.json` 示例：

```json
{
  "dataset_registry": "configs/datasets.json",
  "datasets": [
    "okvqa",
    {
      "name": "captcha",
      "chinese_ratio": 1.0,
      "limit": 5000
    }
  ],
  "output_jsonl": "output/train.jsonl",
  "chinese_ratio": 1.0,
  "seed": 42,
  "limit_per_dataset": null,
  "backend_config": "configs/backend_config.json",
  "sample_prompt_file": "prompts/translate_sample_zh.txt",
  "utterance_prompt_file": "prompts/translate_utterance_zh.txt",
  "emit_raw": true,
  "resume": true
}
```

> 想全部翻成中文就把 `chinese_ratio` 设为 `1.0`（默认配置即如此）；想保留一部分英文再调小，例如 `0.7` 表示每个数据集约 70% 中文、30% 英文。

`output_json`、`images_root`、`failed_path`、`rejected_path`、`report_path` 可省略；默认会按 `output_jsonl` 所在目录推导为 `train.json`、`images/`、`failed.jsonl`、`rejected.jsonl`、`report.json`。

字段说明：

- `dataset_registry`：数据集注册表路径。
- `datasets`：要处理的数据集名字。
- `chinese_ratio`：默认中文比例。
- `seed`：稳定分流 seed，保证断点重跑同一样本仍落到同一种语言。
- `limit_per_dataset`：每个数据集最多输出多少条，`null` 表示不限制。
- `emit_raw`：是否逐数据集写出翻译前英文 `raw.jsonl`，默认 `true`。
- 单个数据集可覆盖比例和数量：

```json
{
  "name": "captcha",
  "chinese_ratio": 1.0,
  "limit": 5000
}
```

### 输入

输入是注册表里的 zip 文件：

```text
data/zips/okvqa.zip
data/zips/chartqa.zip
data/zips/captcha/captcha.zip
```

zip 内部应包含 parquet 文件。程序会从 parquet row 里读取：

- `images` 或 `image`
- `texts`、`messages`、`conversations`
- 如果没有对话，会尝试 `caption`、`description`、`text`

### 运行

```bash
bash scripts/in_translate_zips.sh
```

等价 CLI：

```bash
python -m finevision_to_sharegpt translate-zips --config configs/translate_zips.json
```

### 输出

```text
output/train.jsonl                          # 汇总层：所有数据集的翻译层
output/train.json
output/<dataset_name>/raw.jsonl             # 原始层：翻译前英文记录
output/<dataset_name>/train.jsonl           # 翻译层：中文与保留英文的混合
output/<dataset_name>/train.json
output/images/<dataset_name>/<hash>.<ext>
output/failed.jsonl
output/rejected.jsonl
output/report.json
```

> 每个数据集默认会产出翻译前的 `raw.jsonl`，以及翻译后的 `train.json` / `train.jsonl`；根目录的 `train.json` / `train.jsonl` 是所有数据集翻译层的汇总。`failed.jsonl` / `rejected.jsonl` / `report.json` 仍是单一汇总文件，不生成跨数据集 raw 汇总。

示例记录：

```json
{
  "id": "okvqa:nested/part.parquet:0",
  "images": [
    "images/okvqa/abc123.jpg",
    "images/okvqa/def456.png"
  ],
  "conversations": [
    {
      "from": "human",
      "value": "<image>\n<image>\n问题"
    },
    {
      "from": "gpt",
      "value": "答案"
    }
  ]
}
```

说明：

- 中文样本会调用模型翻译。
- 英文样本不调用模型，直接输出原始英文 ShareGPT。
- 多图会在第一条 human 前插入对应数量的 `<image>`。
- 图片只在 zip 抽取链路里写到 `images/<dataset_name>/`。
- raw 与 train 共用相同的 `images` 路径，图片只保存一次；raw 只生成 JSONL，不生成 `raw.json`。

### 断点续跑

`resume: true` 时会扫描 `output_jsonl` 中已有 `id`，已有样本跳过。

zip 样本 id 规则：

```text
{zip_stem}:{parquet_name}:{row_index}
```

失败样本写入 `failed_path`，默认不算完成，重跑会再次尝试。

raw 使用独立的逐数据集 `id` 去重集合，因此翻译失败后 resume 重试不会重复写 raw。已经完成的样本会在解析前跳过；如果旧任务中途才开启 `emit_raw`，之前完成的英文原文不会自动回填。需要完整 raw 时请设置 `resume: false` 从头运行，或从任务开始就保持 `emit_raw: true`。

## 功能三：解压 zip 不翻译，直接英文导出

用途：从 zip/parquet 抽取图片和英文文本，生成 ShareGPT，不调用模型。

### 需要改的文件

这个功能需要两个配置，直接编辑：

```text
configs/datasets.json
configs/export_zips.json
```

不需要 `backend_config.json`。

### 任务配置

`configs/export_zips.json` 示例：

```json
{
  "dataset_registry": "configs/datasets.json",
  "datasets": ["*"],
  "output_jsonl": "output/train_en.jsonl",
  "limit_per_dataset": null,
  "resume": true
}
```

`output_json`、`images_root`、`rejected_path`、`report_path` 可省略；默认从 `output_jsonl` 推导。

### 输入

同功能二，输入是 `datasets.json` 注册的 zip/parquet 数据集。

### 运行

```bash
bash scripts/in_export_zips.sh
```

等价 CLI：

```bash
python -m finevision_to_sharegpt export-zips --config configs/export_zips.json
```

### 输出

```text
output/train_en.jsonl                       # 所有数据集汇总
output/train_en.json
output/<dataset_name>/train_en.jsonl        # 每个数据集单独一份
output/<dataset_name>/train_en.json
output/images/<dataset_name>/<hash>.<ext>
output/rejected.jsonl
output/report.json
```

和功能二一样，每个数据集单独出一份、外加一份汇总。这个功能不会生成 `raw.jsonl` 或 `failed.jsonl`：其 `train_en.jsonl` 本身就是抽取结果，而且不调用模型。解析不了的样本会进入 `rejected.jsonl`。

## 合并中间 JSONL

`merge` 直接读取一个或多个 JSONL，不使用配置文件：

```bash
python -m finevision_to_sharegpt merge \
  --inputs output/okvqa/train.jsonl output/chartqa/train.jsonl \
  --output output/merged.jsonl
```

行为规则：

- 严格保持 `--inputs` 顺序以及每个文件内部的记录顺序。
- 相同 `id` 只保留第一次出现的记录；没有 `id`（或 `id` 为 `null`）的记录全部保留。
- 不排序、不打乱、不抽样，不改变记录内容。
- 覆盖写出 `output/merged.jsonl`，并自动生成同名 `output/merged.json` 数组。

完成后会打印：

```text
read=<总读入> written=<去重后> duplicates=<去掉的重复数> output=<输出路径>
```

## 多图规则

所有输出统一：

- `images` 数组里有几张图，第一条 human 里就放几个 `<image>`。
- token 连续放在第一条 human 开头。

示例：

```text
<image>
<image>
问题
```

对应：

```json
"images": ["images/a.jpg", "images/b.jpg"]
```

## 低内存和速度设计

当前实现：

- parquet 流式读取。
- backend pool 使用固定并发和有界任务队列，不一次性吃完整数据集。
- 成功结果立即写 JSONL。
- 最终 JSON 从 JSONL 二次流式汇总生成。
- `translate-json` 不搬图片，只读原路径。
- `translate-zips` 和 `export-zips` 才会抽图到 `images/<dataset_name>/`。
- 三个主功能都有 tqdm 进度条；`translate-json` 显示记录总进度，`translate-zips` 和 `export-zips` 显示每个 parquet 的 row 进度，并在后缀展示 processed/written/failed/skipped 等统计。

速度来自：

- 多线程请求。
- 每个 backend 固定并发。
- 多卡注册为多个 backend。
- 多模型注册为多个 backend。
- backend 从共享任务流里动态取任务，快 backend 会自然处理更多样本。

## 常见运行顺序

### 已有 JSON 翻译

1. 编辑 `configs/backend_config.json`
2. 编辑 `configs/translate_json.json`
3. 确认 `images_root` 能找到 JSON 里的图片路径
4. 运行：

```bash
bash scripts/in_translate_json.sh
```

### zip 按比例翻译

1. 编辑 `configs/datasets.json`
2. 编辑 `configs/backend_config.json`
3. 编辑 `configs/translate_zips.json`
4. 运行：

```bash
bash scripts/in_translate_zips.sh
```

### zip 英文导出

1. 编辑 `configs/datasets.json`
2. 编辑 `configs/export_zips.json`
3. 运行：

```bash
bash scripts/in_export_zips.sh
```

### 合并中间文件

```bash
python -m finevision_to_sharegpt merge \
  --inputs output/okvqa/train.jsonl output/chartqa/train.jsonl \
  --output output/merged.jsonl
```

## 快速检查输出

检查 JSONL 行数：

```bash
wc -l output/train.jsonl
```

检查 failed：

```bash
wc -l output/failed.jsonl
```

检查 report：

```bash
cat output/report.json
```

校验 ShareGPT 格式：

```bash
python -m finevision_to_sharegpt validate \
  --input output/train.jsonl \
  --output output/train.clean.jsonl \
  --rejects output/train.format_rejected.jsonl
```
